'use client';

// Você pode criar um componente de Spinner mais elaborado depois
export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p>Carregando...</p>
    </div>
  );
}